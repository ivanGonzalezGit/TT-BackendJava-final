package com.last_mile.wine_n_coffee;

import org.springframework.web.bind.annotation.*;

@RestController
public class ProductController {
/*
    // localhost:8080/

    // POST /products
    @PostMapping("/product")

    // GET /products
    @GetMapping("/product")

    // PUT /products
    @PutMapping("/product")

    // DELETE /products
    @DeleteMapping("/product")


*/
}
